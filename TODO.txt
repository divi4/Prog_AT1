Check if just replace 1 for 0 is faster than
xor


Find if better to replace enumerate with 
another way to access values in list 
incrementally
Determine if better to replace else with
elif style wise


Check if have to directly call the assignment
function or can be called by a 'main' func
instead - ask if cant find in lectures